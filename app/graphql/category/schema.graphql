type Category {
    id: ID!
    name: String!
    description: String
    created_at: Date
    updated_at: Date
    icon: String
}

type Query {
    category(id: ID!): Category
}

type Mutation {
    createCategory (id: ID!, name: String!, description: String, icon: String): Category
}
